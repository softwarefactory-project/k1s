[tox]
envlist = py35

[testenv]
deps =
    pytest
    flake8
    mypy
    openshift
    ansible
whitelist_externals = bash
commands =
    flake8
    mypy --ignore-missing-imports k1s/api.py
    pytest -v tests.py
